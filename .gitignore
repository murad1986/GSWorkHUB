.DS_Store
.obsidian/
__pycache__/
*.pyc
venv/
.venv/
# артефакт стороннего CLI, не заметка
error.log
# личные разрешения агента на этой машине; общие настройки — в .claude/settings.json
.claude/settings.local.json
.ruff_cache/

# настройки редактора, у каждого свои
.vscode/

# Секреты: живут в проекте, но не в git. Склад уезжает на сервер целиком —
# всё, что попало в историю, попадёт туда вместе с ним и останется навсегда.
config/secrets.env
*.env
